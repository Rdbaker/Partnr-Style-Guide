{
  "name": "partnr-style-guide",
  "version": "0.0.2",
  "description": "Partnr's style for web components",
  "scripts": {
    "start": "./node_modules/.bin/gulp heroku:production",
    "postinstall": "if [[ $BUILD_ASSETS -eq \"true\" ]]; then npm run heroku-deploy; fi",
    "prepublish": "npm run build",
    "heroku-deploy": "ENV=production gulp build",
    "build": "ENV=production node ./node_modules/gulp/bin/gulp.js build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-coffee": "^2.3.2",
    "gulp-connect": "^3.2.2",
    "gulp-jade": "^1.1.0",
    "gulp-notify": "^2.2.0",
    "gulp-sass": "^2.2.0",
    "require-dir": "^0.3.0"
  },
  "dependencies": {
    "bootstrap": "^3.3.6",
    "fontawesome": "^4.5.0"
  }
}
