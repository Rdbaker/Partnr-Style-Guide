{
  "name": "partnr-style-guide",
  "version": "0.0.1",
  "description": "Partnr's style for web components",
  "scripts": {
    "start": "./node_modules/.bin/gulp serve",
    "postinstall": "if $BUILD_ASSETS; then npm run build-assets; fi",
    "prepublish": "ENV=production ./node_modules/.bin/gulp build",
    "build-assets": "npm install && npm run prepublish"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-coffee": "^2.3.2",
    "gulp-jade": "^1.1.0",
    "gulp-notify": "^2.2.0",
    "gulp-sass": "^2.2.0",
    "require-dir": "^0.3.0"
  },
  "dependencies": {
    "bootstrap": "^3.3.6",
    "fontawesome": "^4.5.0"
  }
}
